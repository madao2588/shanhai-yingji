2026-05-07: Optimizing backend hardening after repository analysis. Added regression coverage for upload MIME consistency, uploaded file cleanup, and configured Origin handling before implementation.
2026-05-08: Starting long-horizon product polish. First lane is the core beta loop: structured memory creation, visibility/status metadata, local archive/detail/profile-space feedback, then browser and full test verification.
2026-05-08: Continued polish. Main create flow now syncs public/private metadata, date, route, tags, and imported local photos to the backend when logged in. Cloud panel refreshes after create-page publish, cloud memory selection refills the editor/photo list, and dynamic community cards open the shared memory detail view.
2026-05-08: Continued cloud manager polish. Selecting a cloud memory now updates the existing record instead of duplicating it. Added explicit New Memory and Delete Memory controls, with browser coverage for update-in-place, new private draft creation, and selected cloud memory deletion.
2026-05-08: Finalized current beta loop polish pass. Public memory detail now loads and submits comments, profile space merges cloud and local memories, and deleting a selected cloud photo refreshes the manager/community fallback state. Full npm test passes after the pass.
2026-05-08: Continued messages polish. Cross-user interactions now generate actionable message notifications; notification items expose their target public memory, support click/keyboard activation, open the related detail view, and load the comment thread. Full npm test passes after the pass.
2026-05-08: Added notification read-state hardening. Authors can mark their own notifications read through the API, other users cannot, and clicking a memory notification clears the unread indicator while keeping the detail jump working.
2026-05-08: Reworked the Profile tab into a normal app account center. It now shows login/sync state, dynamic local/cloud/interaction counters, grouped content/message/media/security tasks, and fixes mobile screen height so the profile surface is not clipped. Full npm test and production audit pass after the change.
2026-05-08: Continued messages polish after the profile pass. The Messages tab now has an inbox summary, unread/read conversation state, mutable previews, empty-reply validation, and sent-state feedback while preserving backend notification behavior. Focused prototype and UI feedback tests pass; full verification pending.
