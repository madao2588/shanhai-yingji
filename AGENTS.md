# Agent Rules

- 自治执行任务，不要中途问“是否继续”。
- 可以读取、修改代码、运行测试、启动本地服务。
- 遇到测试失败要继续修，直到通过或确认硬阻塞。
- 不要做破坏性操作：不要删库、不要重置 git、不要改生产配置。
- 每次完成后必须汇报：改了什么、跑了什么验证、还有什么风险。
- 长任务必须把进度写到 `.omx/notepad.md`。
- 稳定项目知识必须写到 `.omx/project-memory.json` 或 `.omx/wiki/`。
